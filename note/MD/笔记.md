#### 这个也是免费的：https://pixso.cn/app/drafts

#### 抠图ai工具支持多种文件格式：https://kt.94xy.com/





#### 修改hotos

C:\Windows\System32\drivers\etc文件下

也可win+r执行  drivers



### css

#### 计数器样式

counter-reset： 定义计数器的名称和初始值。

counter-increment：用来标识计数器与实际相关联的范围。

content：用来生成内容，其为`:before`、`:after`或`::before`、`::after`的一个属性。在生成计数器内容，主要配合`counter()`一起使用。

counter()：该函数用来设置插入计数器的值。

:before :after：配合`content`用来生成计数器内容。

**还可以这样**：1，2，3等。第二级列表项目将编号为1.1，1.2，1.3等。第三级项目将是1.1.1，1.1.2，1.1.3，1.2.1，1.2.2，1.2.3

![image-20250804101527693](C:\Users\PC-197\AppData\Roaming\Typora\typora-user-images\image-20250804101527693.png)

### inherit\initial\unset\revert



```
`initial`（默认）
initial 关键字用于设置 CSS 属性为它的默认值，可作用于任何 CSS 样式。（IE 不支持该关键字）
 `inherit`（继承）
部分可继承样式: font-size, font-family, color, text-indent
部分不可继承样式: border, padding, margin, width, height
所有元素可继承：visibility 和 cursor
内联元素可继承：letter-spacing、word-spacing、white-space、line-height、color、font、 font-family、font-size、font-style、font-variant、font-weight、text- decoration、text-transform、direction
块状元素可继承：text-indent和text-align
列表元素可继承：list-style、list-style-type、list-style-position、list-style-image
表格元素可继承：border-collapse

revert：属性应用了该值后，将还原到具有由浏览器或用户创建的自定义样式表（在浏览器侧设置）设置的值
unset: 属性应用了该值后，样式将完全被还原
```

### 虚拟列表

#### 	什么是虚拟列表

​		虚拟列表就是一种假装渲染了所有数据的方式，**但其实只渲染了用户看到可视区的一那部分数据**，可以提升性能

#### 	为什么要用虚拟里表

​		当我们要展示的数据比如一万条，如果都渲染到DOM里，页面就卡的动不了 还可能会崩掉，用虚拟列表的话，比如用户能看到可视区的内容就是10条，那其他的就先不渲染，这样的话 页面就流畅 加载快 内存占用小

#### 	原理

​		首先设置一个““假容器”，再监听滚动位置，算出该显示多少条，最后把这些数据都“挪到可视区里”

#### 	缺点

##### 		每条数据高度必须得一致或者可预估，否则很难算位置

##### 		滚动监听频繁，要加节流，否则也会卡

##### 		复杂组件状态同步难，比如你在第某条填了表单，再滚回来可能被重置了

​			可以选择缓存，或者使用把组件的数据存在全局store里

##### 		滚动穿透、闪屏、白屏

​			问题来源：渲染不及时或者偏移计算不准时，用户滚动到某些区域可能看到‘空白’体验很差

​			解决：

​				1、提前加载“缓冲区”，比如可视区域的上下各加2-5条这样

​				2、优化渲染时间，不在滚动时间中直接setState

​				3、确保数据异步加载提前完成 比如分页加载提前请求

#### 	优点

#### 	注意





web性能



### web安全

#### xss攻击

##### 什么是xss

XSS，是一种**代码注入攻击**。攻击者通过在目标网站上**注入恶意脚本**，使之在用户的浏览器上运行。利用这些恶意脚本，攻击者可获取用户的**敏感信息如Cookie、SessionID**等，进而危害数据安全。

##### 本质是

​	**恶意代码未经过滤**，与网站正常的代码混在一起；**浏览器无法分辨**哪些脚本是可信的，**导致恶意脚本被执行**。

##### 攻击方式

​	分为三种：**存储型 、反射型 、DOM 型**

存储型:它**不需要用户手动触发**，所有浏览者访问页面时都会被XSS

反射性：**主动打开**恶意的 URL 才能生效，攻击者往往会结合多种手段诱导用户点击

DOM性：属于前端自己的bug，他可以访问

| **特征**     | **存储型 XSS**             | **反射型 XSS**                  | **DOM 型 XSS**                       |
| ------------ | -------------------------- | ------------------------------- | ------------------------------------ |
| **存储位置** | 恶意代码存储在服务器数据库 | 恶意代码不存储，仅存在于URL参数 | 恶意代码不存储，存在于URL片段        |
| **触发条件** | 用户访问被感染的页面       | 用户点击包含恶意参数的链接      | 用户点击包含恶意片段(#)的链接        |
| **数据流**   | **服务器** → 用户浏览器    | **服务器**反射参数 → 用户浏览器 | 完全在**客户端**处理（不经过服务器） |
| **持久性**   | 长期存在（直到数据被删除） | 一次性（仅当用户点击链接时）    | 一次性（仅当用户点击链接时）         |
| **攻击范围** | 所有访问该页面的用户       | 仅点击特定链接的用户            | 仅点击特定链接的用户                 |

防御

##### 输入过滤

对**明确的类型**数字、URL、电话号码、邮件，使用`escapeHTML()`可以把用户的输入内容进行转义

对**不明确的类型**最好不要用输入过滤因为当把转义后的数据发送到后端再回到前端，**赋值给一个变量**之后，展示出来的效果会变成**乱码**

##### react对xss

默认转义说有插值内容，他输入script标签也会被认为是一串字符串不会被执行

##### vue对xss

使用模板插值自动编码的，也是会自动转为一串字符串不会被执行

尽量不要使用.innerHTML`、`.outerHTML`、`document.write()

尽量使用 `.textContent`、`.setAttribute()`

##### 设置白名单

使用DOMPutify库来设置白名单

| 安全API                                  | 危险替代品           | 场景           |
| ---------------------------------------- | -------------------- | -------------- |
| `textContent`                            | `innerHTML`          | 插入纯文本内容 |
| `setAttribute`                           | `innerHTML`          | 设置元素属性   |
| `document.createElement` + `appendChild` | `insertAdjacentHTML` | 动态创建节点   |

#### 点击劫持

​		为了做到这一点，攻击者会创建一个将用户的目标网站嵌入到`<iframe>` 元素中的诱饵网站。攻击者的站点会隐藏 `<iframe>`，并对齐一些诱饵元素，使它们与目标网站中触发敏感操作的元素出现在同一个位置。当用户尝试与这些诱饵元素进行交互时，他们会无意中与目标网站交互，并可能被引诱对目标网站执行他们不想要执行的操作。

​			示例:

​				1、创建`<iframe>`标签，然后给这个标签设置 隐藏占位opacity属性。

​				2、将创建好的这个元素跟页面上的button标签对齐覆盖。

#### 什么是 SQL 注入攻击？[¶](https://cheatsheetseries.owasp.org/cheatsheets/SQL_Injection_Prevention_Cheat_Sheet.html#what-is-a-sql-injection-attack)

如果应用程序包含使用字符串连接和用户输入的动态数据库查询，攻击者就可以利用 SQL 注入。为了避免 SQL 注入漏洞，开发人员需要：

1. 停止使用字符串连接编写动态查询或
2. 防止恶意 SQL 输入包含在执行的查询中。

### web性能

在开始优化代码之前，你应该先回答一个问题：“我需要优化什么？”

##### new ResizeObserver

‌**ResizeObserver 是浏览器原生提供的 JavaScript API，专门用于高效监听 DOM 元素的尺寸变化**‌。相比于传统的 `window.resize` 事件，它支持精准感知元素级尺寸变化，且避免了性能损耗问题

### JavaScript

#### 函数

```）
~function (){}()
~:代表（）
加上~就相当于加上了（）所以就能自执行了
```

#### 语法、数据类型

JavaScript 是**区分大小写**的，并使用 **Unicode** 字符集

##### 注释：单行 //  多行 /**/

##### 声明：

​		var ：声明一个变量，可选择将其初始化为一个值。

​		let：声明一个块级作用域的局部变量，可选择将其初始化为一个值。

​		const：声明一个块级作用域的只读命名常量。

#####  变量：

​		JavaScript 标识符通常以字母、下划线（`_`）或者美元符号（`$`）开头；后续的字符也可以是数字（`0`-`9`）。因为 JavaScript 是区分大小写的，所以字母包含从 `A` 到 `Z` 的大写字母和从 `a` 到 `z` 的小写字母。

​			1、使用关键字var ，这个声明的变量是局部和全局的变量

​			2、使用关键字 const let，这个声明的话可以用来声明块级作用域的局部变量



##### 	变量作用域：

​				1、全局作用域：在脚本模式中运行的所有代码的默认作用域

​				2、模块作用域：在模块模式中运行的代码的作用域

​				3、函数作用域：由函数创建的作用域

​					块级作用域：用一对花括号创建的作用域（let、const）



##### 	变量提升

​			用 `var` 声明的变量会被提升，意味着你可以在该变量所在的作用域的任意地方引用该变量，即使还没有到达变量声明的地方。你可以看见 `var` 声明好像被提升到该变量的函数或全局作用域的顶部。然而，如果你在声明变量之前访问该变量，其值总是 `undefined`，因为只有该变量的*声明*和*默认初始化（为 `undefined`）\*被提升，而不是它的值赋值。

```
console.log(x === undefined); // true
var x = 3;

(function () {
  console.log(x); // undefined
  var x = "局部值";
})();
```



​			**由于存在变量提升，一个函数中所有的 `var` 语句应尽可能地放在接近函数顶部的地方。这个最佳实践会提升代码的清晰度**。

​			`let` 和 `const` 是否被提升是个定义争论。在变量声明之前引用块中的变量，将总是抛出 [`ReferenceError`](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/ReferenceError)，因为该变量位于从块的开始到声明所在的“[暂时性死区](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Statements/let#暂时性死区)”中。



##### 	全局作用域

​		在网页中，全局对象是 [`window`](https://developer.mozilla.org/zh-CN/docs/Web/API/Window)，所以你可以用 `window.variable` 语法读取和设置全局变量。在所有的环境中，[`globalThis`](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/globalThis) 变量（其自身也是一个全局变量）可以被用于读取和设置全局变量。这为不同 JavaScript 运行时提供了一个一致的接口

##### 	常量

​		你可以用 [`const`](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Statements/const) 关键字创建一个只读命名常量。常量标识符的语法和任何变量标识符的语法相同：**必须以字母、下划线或美元符号（`$`）开头并可以包含字母、数字或下划线**

​		常量不可以通过赋值来改变其值或在脚本运行时被重新声明。必须为其初始化一个值。常量的作用域规则和 `let` 块级作用域变量的一致。

在同一作用域中，不能使用与变量名或函数名相同的名字来声明常量

​		`const` 仅阻止*重新赋值*，而不阻止*修改*。被赋值为常量的对象的属性是不受保护的，数组的元素也是不受保护的

##### 	数据类型

​		七种[基本](https://developer.mozilla.org/zh-CN/docs/Glossary/Primitive)数据类型：

​				Boolean。`true` 和 `false`。

​				null。一个表示空值的特殊关键字。（因为 JavaScript 是区分大小写的，所以 `null` 和 `Null`、`NULL` 或其他变体是不一样的。）

​				undefined。一个未定义值的顶级属性。

​				Number。整数或浮点数。例如，`42` 或 `3.14159`。

​				BigInt。任意精度的整数。例如，`9007199254740992n`。

​				String。表示文本值的字符序列。例如，`"Howdy"`。

​				Symbol。其实例是唯一且不可变的数据类型

##### 	数字和“”+“”运算符

​		**在使用 `+` 运算符的表达式中涉及数字和字符串，JavaScript 会把数字转换成字符串**

​		**在使用其他运算符时，JavaScript *不*会把数字转换成字符串**

````
x = "答案是 " + 42; // "答案是 42"
y = 42 + " 是答案"; // "42 是答案"
z = "37" + 7; // "377"

-------------------------------------
"37" - 7; // 30
"37" * 7; // 259
````



##### 	字符串转换数字



##### this指向

| 场景                                  | 代码                                                  | `this` 的值                           |
| ------------------------------------- | ----------------------------------------------------- | ------------------------------------- |
| 全局作用域（非严格模式）              | `console.log(this);`                                  | `window`                              |
| 全局作用域（严格模式 `"use strict"`） | `console.log(this);`                                  | `undefined`                           |
| 普通函数（非严格模式）                | `function f(){console.log(this);} f();`               | `window`                              |
| 普通函数（严格模式）                  | `function f(){'use strict'; console.log(this);} f();` | `undefined`                           |
| 箭头函数（全局）                      | `(() => console.log(this))();`                        | **继承外层**，如果在全局就是 `window` |
| 对象方法调用                          | `{ foo(){console.log(this);} }.foo();`                | 调用者对象                            |
| 构造函数                              | `function C(){console.log(this);} new C();`           | 新建的实例对象                        |
| `new Function('return this')()`       |                                                       | `window` ✅ （绕过严格模式）           |
| ES2020 推荐                           | `console.log(globalThis);`                            | `window`                              |

### PS

##### 置灰步骤：

ctrl+e 合并

ctrl+shift+u 加滤镜

##### 全部显示

alt+鼠标左键 ： 自适应到图片都显示出来

##### 裁剪

使用**m快捷键**调出矩形选择工具>>> 框选住你要选择的内容 >>> ctrl+shift+c  >>> ctrl+n >>> ctrl+v 就可

##### 画布大小快捷键

ctrl+alt+c

##### 饱和度快捷键

alt+ctrl+a-z

##### 魔棒选择工具快捷键：w

##### 移动工具快捷键：V

##### 矩形工具快捷键：M

##### 印章工具快捷键：S

##### 渐变工具快捷键：G

##### 旋转快捷键：R

##### 文字工具快捷键：shift+T

##### 修补工具快捷键：J

##### 图框工具快捷键：K

##### 钢笔工具快捷键：P

##### 磁性套索工具快捷键：L







![image-20250908100814323](C:\Users\PC-197\AppData\Roaming\Typora\typora-user-images\image-20250908100814323.png)







### wx小程序

![image-20250904142822887](C:\Users\PC-197\AppData\Roaming\Typora\typora-user-images\image-20250904142822887.png)



重点标识：emp

#### 项目结构：



#### sitemap配置：

用来配置小程序页面是否允许微信索引

"action": "allow",被索引

"action": "disallow",不被索引



小程序配置-全局配置地址**（app.json）**：

https://developers.weixin.qq.com/miniprogram/dev/reference/configuration/app.html

2、更为详情：https://q.qq.com/wiki/develop/miniprogram/frame/dispose.html

| navigationBarBackgroundColor | HexColor | #000000  | 导航栏背景颜色，如 `#000000`                                 |      |
| ---------------------------- | -------- | -------- | ------------------------------------------------------------ | ---- |
| navigationBarTextStyle       | String   | white    | 导航栏标题颜色，仅支持 `black` / `white`                     |      |
| navigationBarTitleText       | String   |          | 导航栏标题文字内容                                           |      |
| navigationStyle              | String   | default  | 导航栏样式，仅支持以下值： `default` 默认样式 `custom` 自定义导航栏，只保留右上角胶囊按钮。参见注 2。 |      |
| backgroundColor              | HexColor | #ffffff  | 窗口的背景色                                                 |      |
| backgroundTextStyle          | String   | dark     | 下拉 loading 的样式，仅支持 `dark` / `light`                 |      |
| backgroundColorTop           | String   | #ffffff  | 顶部窗口的背景色，仅 iOS 支持                                |      |
| backgroundColorBottom        | String   | #ffffff  | 底部窗口的背景色，仅 iOS 支持                                |      |
| enablePullDownRefresh        | Boolean  | false    | 是否开启当前页面的下拉刷新。详见 [Page.onPullDownRefresh](https://q.qq.com/wiki/develop/miniprogram/frame/logic/logic_register_page.html#onPullDownRefresh) |      |
| pageOrientation              | String   | portrait | 屏幕旋转设置，支持 `auto` / `portrait` / `landscape` 。详见 [响应显示区域变化](https://q.qq.com/wiki/develop/miniprogram/frame/view/view_section_change.html) |      |

![img](https://res.wx.qq.com/wxdoc/dist/assets/img/config.344358b1.jpg)

小程序-页面配置地址（组件内的json文件）：

https://developers.weixin.qq.com/miniprogram/dev/reference/configuration/page.html



#### 使用 Page 构造器注册页面

page参数：https://developers.weixin.qq.com/miniprogram/dev/reference/api/Page.html

生命周期狗子函数名称：function( ) { 你的逻辑 }

onLoad：页面创建时执行

onShow:页面出现在前台时执行

onReady:页面首次渲染完毕时执行

onHide:页面从前台变为后台时执行

onUnload:页面销毁时执行

onPullDownRefresh：出发下拉刷新时执行

onReachBottom：页面触底时执行

onShareAppMessage：页面被用户分享时执行

onPageScroll：页面滚动时执行

onResize：页面尺寸变化时执行

onTabItemTap：tab点击时执行

viewTap：事件响应函数

customData：自由数据







giftRecord



#### 自定义组件

详情：https://developers.weixin.qq.com/miniprogram/dev/reference/api/Component.html

json文件当中：配置component，**component 必须是true**

其他组件要是自定义组件要使用，需要再当前json文件当中**usingComponents：{“随便命名”:"自定义组件的路径"}**



#### 接收外部参数、、配置

​	1、需要在js文件当中配置properties **（emp）**

```
properties: {
	myvalue：{
		type：数据类型
		value：""
	}，
	也可：myvalue:数据类型
}
```



组件的生命周期函数-详情链接：https://developers.weixin.qq.com/miniprogram/dev/framework/custom-component/lifetimes.html

```
Component({

  behaviors: [],

  properties: { // emp
    myProperty: { // 属性名
      type: String,
      value: ''
    },
    myProperty2: String // 简化的定义方式
  },
  
  data: {}, // 私有数据，可用于模板渲染

  lifetimes: { 
    // 生命周期函数，可以为函数，或一个在methods段中定义的方法名
    attached: function () { },
    moved: function () { },
    detached: function () { },
  },

  // 生命周期函数，可以为函数，或一个在methods段中定义的方法名
  attached: function () { }, // 此处attached的声明会被lifetimes字段中的声明覆盖
  ready: function() { },

  pageLifetimes: {
    // 组件所在页面的生命周期函数
    show: function () { },
    hide: function () { },
    resize: function () { },
  },

  methods: {
    onMyButtonTap: function(){
      this.setData({
        // 更新属性和数据的方法与更新页面数据的方法类似
      })
    },
    // 内部方法建议以下划线开头
    _myPrivateMethod: function(){
      // 这里将 data.A[0].B 设为 'myPrivateData'
      this.setData({
        'A[0].B': 'myPrivateData'
      })
    },
    _propertyChange: function(newVal, oldVal) {

    }
  }

})
```





### 浅拷贝 (Shallow Copy)

- **定义**：只复制对象的第一层属性，如果属性值是引用类型（如对象、数组），复制的是引用地址，而不是引用指向的实际内容

- **特点**：修改新对象的引用类型属性时，原对象的对应属性也会被修改

  ### Object.assign()

  - 只复制可枚举属性
  - 对于嵌套对象只复制引用

  ### 展开运算符 (...)

  - 与Object.assign()类似

  - 语法更简洁

    ### Array.prototype.slice()、Array.prototype.concat()

    - 适用于数组的浅拷贝

### 深拷贝 (Deep Copy)

- **定义**：完全复制对象的所有层级属性，包括引用类型的属性值，创建全新的引用类型对象
- **特点**：修改新对象的任何属性都不会影响原对象

### JSON.parse(JSON.stringify())

- **优点**：简单易用

- 缺点

  ：

  - 不能复制函数、正则表达式、Date对象
  - 不能处理循环引用
  - 会丢失undefined值

```
function deepClone(obj, hash = new WeakMap()) {
  // 处理null和非对象类型
  if (obj === null || typeof obj !== 'object') {
    return obj;
  }
  
  // 处理日期和正则
  if (obj instanceof Date) {
    return new Date(obj);
  }
  if (obj instanceof RegExp) {
    return new RegExp(obj);
  }
  
  // 处理循环引用
  if (hash.has(obj)) {
    return hash.get(obj);
  }
  
  // 创建相同类型的新对象
  const cloneObj = new obj.constructor();
  hash.set(obj, cloneObj);
  
  // 递归复制所有属性
  for (let key in obj) {
    if (Object.prototype.hasOwnProperty.call(obj, key)) {
      cloneObj[key] = deepClone(obj[key], hash);
    }
  }
  
  return cloneObj;
}
```

- Lodash的`_.cloneDeep()`

- **浅拷贝**：适合简单对象，只关心第一层属性，性能较好
- **深拷贝**：适合复杂嵌套对象，需要完全隔离的场景，但性能开销较大